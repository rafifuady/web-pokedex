/**
 * import depedencies
 */
import { BrowserRouter, Route, Routes } from 'react-router-dom';

/**
 * import pages
 */

function AppRoutes() {
  return (
    <BrowserRouter>
      <Routes>
        <Route path='' element />
      </Routes>
    </BrowserRouter>
  );
}

export default AppRoutes;
