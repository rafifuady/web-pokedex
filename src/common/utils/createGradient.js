export function createGradient(color1, color2) {
  return `linear-gradient(to top, ${color1}, ${color2})`;
}

export function createGradientX(color1, color2) {
  return `linear-gradient(to left, ${color1}, ${color2})`;
}
